# v0.5-train-018-vmdk-extract — flag.vmdk 实战 + 2 bug 根因

> **status**: ✅ done (2026-06-29 22:55 AI 兜底实战 + Owner 决策)
> **触发**: Owner 2026-06-29 22:49 实战 flag.vmdk (面具下的flag writeup), auto-run 跑 4 binary pool 工具 (file/strings/binwalk/exiftool), strings 命中 "Oops,flag_part_two_isn't_here!" 强线索, 但 vmdk 内嵌文件雕不出
> **实战答案**: vmdk → qemu-img convert → raw → foremost 雕 → 2 flag 文件: NULL (brainfuck 加密) + flag_part_two.txt (Ook! 加密)
> **暴露 2 个 bug** (per §5.2 实战判定 + Owner 实战反馈):
> 1. **sevenz_extract spec 文档写错** — 写"支持 30+ 格式" 含 vmdk/vhd/iso/ova, 实测 7z 26.02 不支持 vmdk (CLI + adapter 都报 "Cannot open as archive")
> 2. **foremost 1.5.7 Win length overflow bug** — 跑 vmdk audit 显示 "Length: 8EB" (3MB overflow 到 signed 64-bit), 0 files extracted
> **架构判定**: vmdk 实战 1 道同类 → **不**升架构 (per AGENTS §5.2 ≥ 3 道同类标准), 写训练日志观察

> 📝 **Doc-fix 2026-06-30** (per [v0.5-train-019-7z-version-vmdk.md](v0.5-train-019-7z-version-vmdk.md) + [v0.5-qemu-img-remove.md](v0.5-qemu-img-remove.md)):
> "**7z 不支持 vmdk**" 实测是 **版本差异** — 7z 24.x ~ 26.x 某版本引入回归, **7z 23.01 及以下实测支持 vmdk** (`7z x flag.vmdk` 解出 4 项).
> Owner 当时装的是 **7z 26.02 (x86)**, 所以 7z 报 "Cannot open as archive". 后续切到 **7z 23.01 完整安装** (per `v0.5-7z-layout-migrate`) 后 vmdk 又支持了.
> 本训练日志实战答案 "qemu-img convert" 仍有效 (作为 fallback 路径), 但主路径用 `7z x flag.vmdk` 更轻量 (无需 500MB+ qemu-img NSIS 安装包).

---

## 1. 题目元信息

| 字段 | 值 |
|---|---|
| 题目文件 | `C:\Users\zmzsg\Downloads\56e2e26c700062b832507aa958a4c3f5f68085bb3a3c41bd9ce372288466b0ab\面具下的flag\mianju__foremost\zip\00000934_unzipped\flag.vmdk` |
| 体积 | 3,145,728 bytes (3 MB) |
| 格式 | VMware4 disk image (VMDK 单文件 sparse) |
| 来源 | 面具下的flag writeup (zip 内嵌 vmdk, foremost 雕出 00000934_unzipped/) |
| Fixture 归档 | **不归档** (per §5.4 实战文件不入仓) |
| 答案形式 | 2 flag 文件: NULL (brainfuck 加密) + flag_part_two.txt (Ook! 加密) |

---

## 2. automisc 实测 (per §5.3 step 1)

### 2.1 auto-run journal (4 binary pool 工具)

| 工具 | exit | SP | 关键命中 |
|---|---|---|---|
| file | 0 | 1 | `file_type: VMware4 disk image` |
| strings | 0 | 11 | `L675: Oops,flag_part_two_isn't_here!` (sev=5 强线索) + 10 Base64 关键字 (sev=4) |
| binwalk | 0 | 0 | `0x0 VMware4 disk image` (只识别 magic, 不雕) |
| exiftool | 1 | 3 | 3 keyword 命中 (flag/foremost/flag) |
| **合计** | | **15** | **strings L675 是 vmdk 内嵌 flag_part_two 的强线索** |

### 2.2 sevenz_extract 实战 (Owner 手动触发)

```
=== sevenz_extract ===
exit_code: 2
7-Zip (r) 26.02 (x86) : Igor Pavlov : Public domain : 2026-06-25
...
Extracting archive: ...\flag.vmdk
Can't open as archive: 1
Files: 0
Size: 0
...
[stderr] ERROR: ...\flag.vmdk : Cannot open the file as archive
suspicious_points (4): [5] keyword: flag / [5] keyword: foremost / [5] keyword: flag / [3] archive_error: 7z x: cannot open the file as archive
```

**根因**: 7z 26.02 binary **不支持 vmdk 格式** (7z 官方支持列表不含 vmdk). adapter 设计正确, 错在 `v0.5-sevenz-extract.md` spec 描述 "支持 30+ 格式: ... vmdk/vhd/wim/iso/ova ..." — **写 spec 时未实测验证**.

### 2.3 foremost Win 实战 (Owner 手动触发)

```
foremost -t all -i flag.vmdk -o foremost_test/
Foremost started at Mon Jun 29 22:51:20 2026
...
Length: 3 MB (8640808836106551296 bytes)  <-- 长度 overflow!
...
0 FILES EXTRACTED
```

**根因**: `foremost 1.5.7` Win port **64-bit length overflow bug** — 3MB 被解释为 signed 64-bit → 8EB, 0 files extracted. 这是 2017 年后 foremost Win fork 未修的 bug, 跟 automisc 无关. (v0.5-platform-extend-tools 装的 raddyfiy fork)

---

## 3. AI 兜底实战 (per §5.3 step 3, Owner 实测)

### 3.1 vmdk 实战解法 (Owner 决策 qemu-img convert)

**vmdk 是 VMware 虚拟磁盘, 实战解法**:

```
qemu-img convert -f vmdk -O raw flag.vmdk flag.raw
foremost -t all -i flag.raw -o flag_extracted/
# 雕出 2 flag 文件
```

**Q1 = 装 qemu-img 到 extend-tools/bin/win-x64/?** (Owner 22:55 决策"qemu-img convert 比较实际", 但**没**正式拍板装)

### 3.2 实战结果

| 文件 | 内容 | 加密 | 实战解码 |
|---|---|---|---|
| NULL | brainfuck 加密文本 | brainfuck | GUI 解密工具 1 分类 → brainfuck decoder (per v0.5-cipher-decoders) |
| flag_part_two.txt | Ook! 加密文本 | Ook! | GUI 解密工具 1 分类 → Ook! decoder (per v0.5-cipher-decoders) |

(per Owner 22:55 实战答案)

### 3.3 实战工具链 (Owner 实战)

| 步骤 | 工具 | 备注 |
|---|---|---|
| 1. 解 vmdk | qemu-img convert | Win 端**没**装, Owner 提议装 extend-tools/ |
| 2. 雕 raw | foremost -t all | raw 格式 foremost 1.5.7 Win OK (无 length overflow, raw length 真实) |
| 3. 解 NULL brainfuck | GUI brainfuck decoder | v0.5-cipher-decoders 已支持 |
| 4. 解 flag_part_two.txt Ook! | GUI Ook! decoder | v0.5-cipher-decoders 已支持 |

---

## 4. 暴露 bug + 修复建议 (per §5.3 step 4)

### 4.1 Bug 1: sevenz_extract spec 文档写错 ⚠️ 必修

**位置**: `upgrade/v0.5-sevenz-extract.md` 描述段

**错误文案**:
> 30+ 格式: zip / 7z / rar / tar / gz / bz2 / xz / wim / iso / **vmdk** / **vdi** / **vhd** / **ova** ...

**实际 7z 26.02 支持** (per 7z 官方文档 + Win CLI 实测):
- ✅ zip / 7z / tar / gz / bz2 / xz / wim / rar
- ❌ **vmdk / vdi / vhd / iso / ova** (7z 26.02 不支持, 需 vhd-tool 单独工具 / 或 vmdk 走 qemu-img convert)

**修复** (3 处 spec 文档同步, 0 代码改动):
- spec 描述段 "30+ 格式" 改成 "8 主流格式: zip/7z/rar/tar/gz/bz2/xz/wim"
- spec 实施清单删 "vmdk/vdi/vhd" 引用
- spec 工具状态表 vmdk 行 ❌ pending + 备注 "7z 不支持, 走 qemu-img convert 路径"
- (类似 v0.5-zipcrack-doc-update 模式: 0 代码改动, 纯文档同步)

**Owner 决策点**: 修 spec 写错? (Y 拍板 → 写 spec fix + commit)

### 4.2 Bug 2: foremost 1.5.7 Win length overflow ⚠️ 不在 automisc 范围

**位置**: `extend-tools/bin/win-x64/foremost.exe` (Win port 第三方 fork, raddyfiy)

**根因**: Win 端 foremost 1.5.7 长度字段 signed 64-bit overflow, macOS / Linux 不复现. **不是 automisc bug**, 是 Win port bug.

**修复路径** (3 选项, Owner 决策):
- **A. 升级 foremost Win port** (v1.5.8+): 等 raddyfiy 修; 时间不定
- **B. 装 qemu-img + 强引导"vmdk 先 convert raw 再雕"**: spec 实战建议加一行
- **C. 不修**: Win 端 foremost 雕 vmdk 失败但其他格式 (jpg/zip/doc) OK, 实战 1 道同类不升架构

**架构判定**: per AGENTS §5.2, Win foremost bug 实战 1 道同类, **不升架构**, 观察后续实战.

### 4.3 Bug 3 (上 turn): race condition 旧 steghide 残留 ⚠️ 待 Owner Y 选 A/B/C

**位置**: `MainWindow._on_new_file_selected` line 474-486 停旧 runner 机制

**根因**: `runner.wait(2000)` 等 2s 强制返回, 旧 FindSuspiciousRunner 的 steghide subprocess 继续跑, 30s timeout 后 emit tool_finished 写入新 output 区, 导致"5 个 archive 段被压到顶部, steghide 段在底部"误读

**Owner 22:30 实战 1 道同类 (拖 jpg → 拖 zip)**, 仍待 Owner 选 A/B/C 修复方案

---

## 5. 架构判定 (per §5.2)

| Bug | 触发模式 | 架构判定 |
|---|---|---|
| sevenz_extract spec 文档写错 | 1 道同类 (v0.5-train-018) | ❌ **不升架构**, spec 文档同步 (per v0.5-zipcrack-doc-update 模式 0 代码) |
| foremost Win length overflow | 1 道同类 (v0.5-train-018) | ❌ **不升架构**, Win port 第三方 bug, 等社区修 |
| race condition 旧 steghide 残留 | 1 道同类 (Owner 22:30) | ❌ **不升架构**, 待 Owner Y 选 A/B/C |

**3 个 bug 都 1 道同类, 都不升架构**:
- spec 文档同步 (0 代码) — Owner Y 拍板即修
- Win port bug 暂缓, 写实战建议 (vmdk 先 convert raw)
- race condition 待 Owner 选 A/B/C 修法

**未来 ≥ 3 道同类触发架构升级**:
- foremost Win 雕大文件 (>2GB) 实战再 hit → 升 `v0.5-foremost-win-replace` 架构 (用 binwalk v3 替代)
- vmdk/qcow2 实战 ≥ 3 道 → 升 `v0.5-vmdk-chain` 架构 (装 qemu-img + 新建 adapter + 自动 convert + foremost)

---

## 6. 实战产物

| 文件 | 大小 | 说明 |
|---|---|---|
| 训练日志（本文件） | ~9KB | v0.5-train-018 |
| 架构升级 spec | — | **不**起草, 1 道同类不升架构 |
| sevenz_extract spec 文档同步 | TBD | 待 Owner Y 拍板修 spec |
| race condition spec | TBD | 待 Owner 选 A/B/C 修法 |

---

## 7. 历史实战交叉引用

- **vmdk 实战**:
  - 本日志 v0.5-train-018 (vmdk 雕失败, 走 qemu-img convert)
  - 未来 v0.5-train-NNN: vmdk/qcow2/raw 实战继续 hit, 累计 ≥ 3 道再升架构
- **spec 文档同步模式**:
  - v0.5-zipcrack-doc-update (zipcrack.py 写错, 0 代码 spec 同步) — 同模式
- **race condition 触发**:
  - v0.5-train-015 steghide 30s timeout 实战 (per 同期) — 同一 steghide 关联 bug

---

## 8. 时间线

| 事件 | 时间 |
|---|---|
| Owner 拖 flag.vmdk + auto-run 跑 binary pool 4 工具 | 2026-06-29 22:49 |
| Owner 手动点 sevenz_extract 跑 vmdk 失败 | 2026-06-29 22:49 |
| Owner 手动跑 foremost 雕 vmdk 0 files | 2026-06-29 22:51 |
| Owner 决策 qemu-img convert 实战解法 + 给出 2 flag 文件内容 | 2026-06-29 22:55 |
| 训练日志归档 | 2026-06-29 22:55 |
| 修 spec 写错 + race condition 修复 (待 Owner Y) | (待办) |

---

> **本日志归档完**. 修 spec 写错 + race condition 修复待 Owner 拍板.
