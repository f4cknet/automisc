# v0.5-windows-only — 项目定位收窄为 Windows only（治理变更 v3.3）

> **状态**：🔄 in-progress（per Owner 2026-06-27 23:25 Y 拍板方向 + 23:29 Y 签字 PR1 实施中）
> **触发**：Owner Win 端 `automisc-gui` 已启动成功 + `extend-tools/` 是 Windows `.exe` → 项目实际是 Win 优先/only
> **类型**：**治理变更 v3.3**（per `AGENTS.md §8`）— `§2.3 multi-platform (macOS + Windows)` → **`Windows only`**
> **回退策略**：**方案 Y · forward commit 收窄**（保留 v3.2 的 `extend-tools/` 骨架，删 darwin 代码路径 + 文档改写）
> **影响面**：治理 3 文件 + 代码 ~25 文件 + 测试 ~8 文件 + `extend-tools/` 1 文件

---

## 1. 背景与动机

### 1.1 v3.2 (1 小时前) 做了什么

2026-06-27 21:54 Owner 拍板 v3.2 治理变更（`commit 264597e`）：`§2.3 macOS only` → `multi-platform (macOS + Windows)`。落地：

- 新建 `extend-tools/` 目录（`manifest.yaml` + `install.ps1` + `install.sh` + `bin/win-x64/`）
- 新建 `src/automisc/tools/paths.py:resolve_tool_binary`（PATH 优先 → `extend-tools/bin/<platform>/` fallback）
- 改 `src/automisc/tools/base.py` darwin 分支显式追加 Homebrew 路径
- 5 个 adapter 改 binary 字段（binwalk / exiftool / foremost / sevenz / sevenz_extract）

**v3.2 已 push 远端**（per Owner 22:59 Y 拍板）。

### 1.2 v3.3 触发 (Owner 2026-06-27 23:21)

Owner 在 Win 端实测 `automisc-gui` 启动成功（PySide6 GUI 真起来），确认：

- ✅ GUI 在 Windows 真能起（不再是 "macOS only" 元数据 bug）
- ✅ `extend-tools/` 装的是 Windows `.exe` binary（binwalk.exe / exiftool(-k).exe / 7zr.exe / foremost.exe）
- ❓ 但代码里仍残留 macOS 路径探测（`base.py` Homebrew 分支 + `paths.py` 平台映射）+ 文档元数据（"macOS only" docstring）

**Owner 结论**：工具链实际是 Win only → **回退跨平台承诺**，删 macOS 代码路径 + 改文档为 "Windows only"，避免维护两份死路径。

### 1.3 关键约束

- **AGENTS.md §1 铁律 4** · 未验证 = 未完成 —— 6 关全绿（合 main + 单测 + GUI 集成 + smoke + Owner 自审 + 文档同步）
- **AGENTS.md §2.1 粒度** —— 单 PR ≤ 400 行；GUI 改 / Core 改分离
- **AGENTS.md §8 治理变更流程** —— 提案 + Owner 自审 + §9 变更日志 + 通知
- **`extend-tools/bin/win-x64/` 已落地** —— 本 spec 不动这个目录本身（保留）

---

## 2. 影响面（按改动类型分组）

### 2.A 治理 / 顶层文档（必改）

| 文件 | 改动 | 估行数 |
|---|---|---|
| `AGENTS.md` 顶行 | "跨平台 extend-tools/" → "Windows only · extend-tools/" | 1 |
| `AGENTS.md §2.3` | `multi-platform (macOS + Windows)` → **`Windows only`**（移除 macOS / Linux 兼容语句） | ~5 |
| `AGENTS.md §4` | "macOS 紧急修复（PySide6 兼容）" → "Windows 紧急修复" | 2 |
| `AGENTS.md §9` | 加 **v3.3** 变更日志（保持最近 4 条） | ~5 |
| `README.md` | 项目定位段 "macOS GUI 半自动化 CTF Misc 工具箱" → "Windows GUI" | ~5 |
| `pyproject.toml` | description 段 "macOS" → "Windows" | ~3 |
| `tools.md` §3.x | 多处 "macOS 自带" / "brew install" 描述改写 | ~30 |

### 2.B Core 平台探测逻辑（清理代码）

| 文件 | 改动 |
|---|---|
| `src/automisc/tools/paths.py:30-34` | 删 `_PLATFORM_DIR` 映射（`"darwin"/"linux"`），hardcode `"win-x64"` |
| `src/automisc/tools/paths.py:46-48` | `exe_suffix` 直接 `return ".exe"` |
| `src/automisc/tools/base.py:136-143` | 删 `_run_subprocess` darwin 分支 |
| `src/automisc/tools/base.py:189-194` | 删 `_run_subprocess_with_input` darwin 分支 |

### 2.C CLI / GUI 元数据（docstring 改写）

| 文件 | 改动 |
|---|---|
| `src/automisc/__main__.py:4` | 删 docstring "(macOS only)" |
| `src/automisc/__main__.py:59` | 删 docstring "(macOS only)" |
| `src/automisc/__main__.py:270` | 改 `help="启动 PySide6 GUI 主窗口"` |
| `src/automisc/gui/main_window.py:9` | 删 "(per AGENTS.md §2.4)" macOS only 注释 |
| `src/automisc/gui/main_window.py:543` | "macOS GUI 半自动化..." → "Windows GUI 半自动化..." |

### 2.D `extend-tools/` 平台无关化

| 文件 | 改动 |
|---|---|
| `extend-tools/install.sh` | **删**（保留 `install.ps1`） |
| `extend-tools/README.md` | 删 macOS / Linux 部分，只留 Windows |
| `extend-tools/manifest.yaml` | 删 darwin / linux 平台字段（如有） |

### 2.E 工具 adapter 描述（brew 注释清理）

| 文件 | 改动 |
|---|---|
| `src/automisc/tools/steganography/image/steghide.py` | 删 "(macOS 现代 fork stegseek)" + 改 brew 注释 |
| `src/automisc/tools/steganography/audio/steghide_audio.py` | 删 macOS 注释 |
| `src/automisc/tools/steganography/audio/sox.py` | 改 macOS soxi 注释 |
| `src/automisc/tools/shared/dd.py` | 改 macOS BSD dd 注释 |
| `src/automisc/tools/misc/brainteaser/zbar.py` | 改 brew install 注释 |
| `src/automisc/tools/misc/archive/unzip.py` | 改 "macOS 自带" 描述 |
| `src/automisc/tools/misc/archive/john.py` | 删 macOS + brew 路径 |
| `src/automisc/tools/forensics/network/tshark.py` | 改 macOS 注释 |
| `src/automisc/tools/forensics/network/tcpdump.py` | 改描述 |
| `src/automisc/tools/forensics/memory/vol.py` | 改 macOS 注释 |
| `src/automisc/tools/forensics/log/grep.py` | 改描述 |
| `src/automisc/tools/forensics/log/evtx_dump.py` | 改 macOS 注释 |

### 2.F Core 业务层（brew 路径 / macOS 兼容代码）

| 文件 | 改动 |
|---|---|
| `src/automisc/core/utils/output_path.py` | 删 macOS tmp symlink 兼容代码（行 9, 42, 67, 182-183） |
| `src/automisc/core/decoders/coords_to_qr.py` | 改 brew install 注释 |
| `src/automisc/core/decoders/base64_image.py` | 改 macOS 依赖注释 |
| `src/automisc/core/actions/stegseek.py` | 删 macOS 优先逻辑 |
| `src/automisc/core/actions/rar_chain.py` | 删 macOS + brew 路径 |

### 2.G 测试层（macOS-only 注释 / 测试逻辑）

| 文件 | 改动 |
|---|---|
| `tests/unit/tools/steganography/image/test_steghide.py` | 删 macOS-only 测试逻辑（行 40-71） |
| `tests/unit/tools/shared/test_foremost.py` | 改 macOS 注释 |
| `tests/unit/tools/shared/test_file.py` | 改 macOS 注释 |
| `tests/unit/tools/shared/test_exiftool.py` | 改 macOS 注释 |
| `tests/unit/core/utils/test_output_path.py` | 删 macOS tmp symlink 测试 |
| `tests/unit/core/decoders/test_coords_to_qr.py` | 删 macOS 注释 |
| `tests/unit/core/actions/test_hex_router.py` | 删 macOS 注释 |
| `tests/unit/test_pr1_integration.py` | 删 macOS 注释 |

---

## 3. PR 拆解（per §2.1 单 PR ≤ 400 行）

| PR | 内容 | 估算行数 | 备注 |
|---|---|---|---|
| **PR1** | 治理变更：AGENTS.md / README / pyproject / tools.md / `upgrade.md` 索引行 + 本提案 status ✅ | ~150 | **走 §8 流程**，需 Owner 自审后合 main |
| **PR2** | Core 平台探测清理（2.B） | ~80 | paths.py + base.py 平台分支 |
| **PR3** | CLI + GUI 元数据（2.C） | ~30 | help / docstring 改写 |
| **PR4** | `extend-tools/` 平台无关化（2.D） | ~50 | 删 install.sh + 改 README |
| **PR5** | adapter 描述清理（2.E） | ~200 | ~10 个文件小改 |
| **PR6** | Core 业务层（2.F） | ~150 | `output_path.py` 可大胆删 macOS tmp 代码 |
| **PR7** | 测试层清理（2.G） | ~300 | 多文件注释精简 |

每个 PR 单独 commit（per §1 铁律 3 · 任务有状态）。PR 顺序：1 → 2 → 3 → 4 → 5 → 6 → 7。

---

## 4. 回退策略（已拍板）

### 方案 X · revert `264597e` ❌

```bash
git revert 264597e  # 回退 v3.2 commit
```

**问题**：丢掉 1 小时 `extend-tools/` 目录建设 + `manifest.yaml` + `install.ps1`。`extend-tools/bin/win-x64/` 已经被 Owner 实测 smoke 跑通（binwalk / exiftool / 7z / foremost 全 OK）。

### 方案 Y · forward commit 收窄 ✅ 推荐

不 revert v3.2，**保留 `extend-tools/` 目录本身**（Windows binary 是好的），只：

- 删 darwin 代码路径（`base.py` Homebrew 分支 + `paths.py` 平台映射）
- 改文档为 "Windows only"
- 删 `extend-tools/install.sh`（不再需要 macOS 装脚本）

git 历史清楚显示 "v3.2 跨平台骨架 → v3.3 收窄为 Win only" 完整脉络。

---

## 5. 决策点（待 Owner 拍板）

| # | 决策点 | 推荐 | 备注 |
|---|---|---|---|
| **Q1** | 回退策略 | **Y · forward commit**（已拍） | 保留 extend-tools/，删 darwin 代码 |
| **Q2** | `extend-tools/install.sh` | **删** | Win-only 无意义 |
| **Q3** | `paths.py:platform_subdir()` 函数 | **保留 API 但只返 "win-x64"** | 向后兼容（已被 5+ adapter 引用） |
| **Q4** | `output_path.py` macOS tmp symlink 兼容代码 | **删** | Win-only 不需要 |
| **Q5** | `stegseek.py` macOS 优先逻辑 | **改 Windows 优先**（stegseek → steghide fallback） | Win 优先 stegseek（更现代） |
| **Q6** | `rar_chain.py` john brew 路径 | **删** | Win 用 john-jumbo 不在 Homebrew |
| **Q7** | `test_steghide.py` macOS-only 测试 | **改 Windows-only 描述** | 跟现状匹配 |
| **Q8** | 是否合并 PR1-7 | **拆分 7 个独立 PR**（per §2.1） | 单 PR ≤ 400 行 |

---

## 6. 验收标准（per §1 铁律 4 · 6 关）

1. **合 main** —— PR1-7 全合 main（每个独立 commit）
2. **单测全绿** —— `pytest tests/` 通过（不退步 baseline 1027 passed 66 skipped）
3. **GUI 改跑集成** —— `automisc-gui` 在 Win 端起得来 + 4 核心工具菜单 `✓`（binwalk/exiftool/7z/foremost）
4. **真实样本 smoke** —— 跑 `Challenge/QR_code.png` 拿 flag `CTF{vjpw_wnoei}` + `Challenge/steg.png` 拿 flag `st3g0_saurus_wr3cks`（baseline）
5. **Owner 自审** —— Owner review PR1-7 全过
6. **文档同步** —— `AGENTS.md §9` v3.3 变更日志 + 本提案 status ✅ + `tools.md` 同步

---

## 7. 治理变更流程（per §8）

1. ✅ Owner 起草变更提案（本文件）
2. ✅ AI Agent 写 `upgrade.md` 索引行（⏳ 状态）
3. ⏳ AI Agent 等 Owner Y 签字
4. ⏳ AI Agent 开 PR1（治理 + 文档同步）
5. ⏳ PR1 合 main → §9 变更日志生效
6. ⏳ PR2-7 顺序推进

---

## 8. 风险 & 注意事项

- **v3.2 → v3.3 间隔 1 小时** —— 不寻常但合理（Owner 实战验证 C 已成功后立刻收窄）
- **`upgrade/v0.5-platform-extend-tools.md` 历史文档不动** —— 作为治理变更完整记录保留；仅在文档头部加 "**被 v3.3 收窄为 Windows only**" 注脚
- **测试层可能引入 fail** —— `output_path.py` 删 macOS 兼容代码后，相关测试要审；预期 **0 新失败**（baseline 1027 passed 66 skipped）
- **远端 push 权限** —— PR 合 main 后 per §2.4 AI Agent 必须询问 Owner 才能 `git push` / `gh pr merge`（本项目单 Owner 直接 main commit + push）
- **macOS 用户如回访** —— 项目 README 明确写 "Windows only"，需要时去 fork 老 v3.2 分支

---

## 9. 时间线 / 里程碑

| 阶段 | 状态 | ETA |
|---|---|---|
| 提案文档起草 | ✅ 完成 | 2026-06-27 23:25 |
| Owner 签字 | ⏳ 等 Owner Y | 2026-06-27 23:30 |
| PR1（治理 + 文档） | ⏳ 待开 | 2026-06-27 23:45 |
| PR2-7（代码 + 测试） | ⏳ 待开 | 2026-06-28 |
| PR1-7 合 main + push 远端 | ⏳ 待 push | 2026-06-28 |
| v3.3 落地 + smoke 全绿 | ⏳ | 2026-06-28 |

---

> **本 spec 等 Owner Y 签字后开 PR1**。
