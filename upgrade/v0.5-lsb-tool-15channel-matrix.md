# v0.5-lsb-tool-15channel-matrix — lsb_tool 2 段 LSB matrix (实战 v4 最终版)

> **创建时间**：2026-06-28 17:25
> **状态**：🟢 active (main 6 commits 落地: `f23be60` zero_aware / `38f93a9` v2 15ch matrix 旧版 / `82a34db` v2 KEEP+ADD 旧版 / `1119992` docs / `1453ba2` v3 8×15 matrix / `52833d4` docs / **v4 Commit 6: 2 段 LSB matrix**)
> **触发**：v0.5-train-016 — Owner 实战反馈 lsb_tool 输出 UX
> **实战修订 4 次历史** (Owner 反复迭代方向):
>   - **v1** (17:25): KEEP+ADD 8×6 + 15ch 双 matrix → journal 3KB, 15ch 截断不显示, ❌
>   - **v2** (18:06): REPLACE 8×6 + 精简 7 通道 + 中文段标题 → 通道砍太多, ⚠️
>   - **v3** (18:13): 8 bit × 15 channel matrix → bit=0/1~6 全保留, 横向 15 通道 → **实施后实战又发现 8 bit × 15 矩阵太复杂** ⚠️
>   - **v4** (22:13, 最终): **2 段 LSB matrix** = `===LSB_TOOL===` 总标题 + "LSB 0 bit" 段 (12 多通道) + "单通道0-7bit" 段 (3 单通道) → **简洁实战 UX** ✅
> **关联 spec**: `v0.5-lsb-tool-unify` (Phase 1-4 已落地, 本 spec 是 Phase 9 v4 实战最终版)
> **关联 spec (扩展)**: `v0.5-lsb-tool-bitplane-preview-matrix` (8 bit × 6 perm matrix 函数被本 spec 替代)

---

## 1. 背景

### 1.1 UX Gap: lsb_tool vs 随波逐流 视图对比 (4 轮反馈历史)

**Owner 2026-06-28 17:18 实战反馈**:
> "lsb_tool auto OK 但没出 SP, 切到 随波逐流 后 RGB 通道直接出明文"

**v1 → v2 → v3 → v4 实战迭代** (Owner 每次反馈都改方向):

| 版本 | Owner 反馈 | 决策 | 实战结果 |
|---|---|---|---|
| v1 | "改成：15 通道，LSB+MSB 增强" | KEEP+ADD 8×6 + 15ch 双 matrix | journal 3KB, 15ch 截断不显示 ❌ |
| v2 | "希望效果（右图）: ===LSB_TOOL=== + LSB检测 + 7 通道 + MSB检测 + 7 通道" | REPLACE 8×6 + 精简 7 通道 | 通道砍太多 ⚠️ |
| v3 | "通道不要变，还是 LSB 和 MSB 分别 15 通道。横向头部 RGB/RBG/GRB/GBR/BRG 看不明白" | 8 bit × 15 channel matrix | 太复杂 (8×15=120 cells) ⚠️ |
| **v4 (最终)** | **"LSB/MSB 我只要 0 比特位，不看列，只看行: ===LSB_TOOL=== + LSB 0 bit (12 通道) + 单通道0-7bit (3 单通道)"** | **2 段 LSB matrix** | ✅ |

**v4 实战效果** (per Owner 期望):
```
===LSB_TOOL===

LSB 0 bit
RGB:Hey! I think we can write safely in this file witho  <==
BRG:$3..$..54<........{...y..3...4...5.;.4..2....?
RBG:$S.@I xX.n] z. b..@w.d..@u.jS.t..n..dY.@V.lS z..d_
BGR:H5|..H.3,..H..H..k.}Z.u..yL..i.!,....3^.=,.4H..Y.?
GRB:.h. $..dY.f..h..`. z..xS r..h..@Y.@r.d. jY.h..d..n
GBR:.X.@.H.R...H..H...@|..x.@t.....@.....R.@\..XH....^
RG0:..Z>v7...7B#.....B?6w.. ?.....6.bG^.Ur>........
R0B:.=B.r.Z.z(J>CxC.CyZ>`.X.B.B]R(z.B.p.[.Z(j;B.B.X.[.
0GB:..@....@....C...CQ........B.................B.r...
R00:..Z>v7...7B#.....B?6w.. ?.....6.bG^.Ur>........
0G0:.......................P........S..............
00B:...............................................

单通道0-7bit
R:.................................................
G:. ..
B:....
```

### 1.2 v4 关键决策 (Owner 22:13 第四次反馈 + 22:16 4 项确认)

| 项 | 决策 |
|---|---|
| 结构 | 2 段: `===LSB_TOOL===` 总标题 + "LSB 0 bit" 段 (12 多通道) + "单通道0-7bit" 段 (3 单通道) |
| 段 1 (LSB 0 bit) | 12 多通道 × bit 0 字节流 preview (per 随波逐流 风格) |
| 段 2 (单通道0-7bit) | 3 单通道 (R/G/B) × 完整 8 bit plane 字节流 preview (per v0.5-train-010 N=NP 模式) |
| 12 通道顺序 | RGB/BRG/RBG/BGR/GRB/GBR/RG0/R0B/0GB/R00/0G0/00B (per Owner 列表) |
| 关键字 `<==` | 保留 |
| preview 字节数 | 50 (per Owner Q4) |
| bit 1~7 行 | 不显示 (per Owner "1-7bit 位忽略") |

**为什么不显示 bit 1~7**: 实战 LSB 隐写 95% 用 bit 0 (LSB), MSB (bit 7) 罕见, mid-bit (bit 1~6) 实战 < 5%. 显示 bit 1~7 是噪音, 不实战价值.

**为什么单通道用 8 bit plane (而不是只 bit 0)**: per v0.5-train-010 N=NP 模式实战价值 —— 单通道 R/G/B 完整 8 bit 拼字节流, 用于 entropy + unique count 异常检测 (G 通道 8 bit plane entropy 5.68 unique 256/256 满 = N=NP 强信号).

---

## 2. 范围

### 2.1 改动文件清单 (v4 最终)

| 类别 | 文件 | 操作 | LOC 影响 |
|---|---|---|---|
| **改 Core** | `src/automisc/core/actions/lsb_tool_common.py:_build_bit_plane_preview_matrix` | 删 (v3 替代品, v4 不再需要) | -50 |
| **改 Core** | `src/automisc/core/actions/lsb_tool_common.py:_format_matrix_for_journal` | 删 (同上) | -40 |
| **新增 Core** | `src/automisc/core/actions/lsb_tool_common.py:_LSB_12_CHANNELS` | 新建: 12 多通道顺序定义 | +15 |
| **新增 Core** | `src/automisc/core/actions/lsb_tool_common.py:_SINGLE_3_CHANNELS` | 新建: 3 单通道顺序定义 | +5 |
| **新增 Core** | `src/automisc/core/actions/lsb_tool_common.py:_build_lsb_2segment_matrix` | 新建: 2 段 matrix 生成 | +60 |
| **新增 Core** | `src/automisc/core/actions/lsb_tool_common.py:_format_lsb_2segment_for_journal` | 新建: 2 段 matrix 渲染 | +30 |
| **改 Core** | `src/automisc/core/actions/lsb_tool.py:_run_detect` | 改: 用 _build_lsb_2segment_matrix / _format_lsb_2segment_for_journal | -15/+5 |
| **改测试** | `tests/unit/core/actions/test_lsb_tool_common.py` | 改: TestBuildBitPlanePreviewMatrix / TestFormatMatrixForJournal → TestBuildLsb2SegmentMatrix / TestFormatLsb2SegmentForJournal | -110/+90 |
| **改测试** | `tests/integration/test_lsb_tool_regression.py` | 改: 期望 2 段结构 + 新段标题 | -10/+10 |

**净 LOC**: ~ -20 (清理 v3 旧代码 + 替换为 v4 新实现)

### 2.2 不动 (vs v1/v2/v3 决策变更)

- `_extract_lsb_byte_stream_zero_aware` (Commit 1) —— 保留, 给 _build_lsb_2segment_matrix 段 1 提供 '0' 通道支持 (RG0/R0B/0GB/R00/0G0/00B)
- `_extract_lsb_byte_stream` / `_channel_8bit_byte_stream` / `_ascii_preview` / `_PERMUTATIONS` / `_SCAN_ORDERS` / `_VALID_*` / `_MAGIC_PREFIXES` 常量 —— 保留
- `_15_CHANNELS` (15 通道全集合, source of truth) —— 保留 (虽然 v4 不用全部, 但保留作为其他模块复用)
- `lsb_tool_adapter.py` / `lsb_tool_dialog.py` / `auto_runner.py` —— 不动
- **预设相关** `_detect_single_combo` / `_detect_all_combos` / `_detect_np_mode` / `_detect_channel_anomaly` —— 不动 (仍走 preset 路径, 跟 v4 2-segment 矩阵并存)

---

## 3. 详细设计 (v4 最终)

### 3.1 `_LSB_12_CHANNELS` + `_SINGLE_3_CHANNELS` (顺序定义)

```python
# 12 多通道 LSB 0 bit 段 (per Owner 2026-06-28 22:13 实战 v4 修订)
_LSB_12_CHANNELS: list[tuple[str, list[str]]] = [
    ("RGB", ["R", "G", "B"]),
    ("BRG", ["B", "R", "G"]),
    ("RBG", ["R", "B", "G"]),
    ("BGR", ["B", "G", "R"]),
    ("GRB", ["G", "R", "B"]),
    ("GBR", ["G", "B", "R"]),
    ("RG0", ["R", "G", "0"]),
    ("R0B", ["R", "0", "B"]),
    ("0GB", ["0", "G", "B"]),
    ("R00", ["R", "0", "0"]),
    ("0G0", ["0", "G", "0"]),
    ("00B", ["0", "0", "B"]),
]

# 3 单通道 (单通道段用, 显示完整 8 bit plane 字节流)
_SINGLE_3_CHANNELS: list[str] = ["R", "G", "B"]
```

### 3.2 `_build_lsb_2segment_matrix`

```python
def _build_lsb_2segment_matrix(
    img_array, n_bytes=50, scan_order="row", byte_bit_order="msb",
):
    """2 段 LSB matrix (per v4 实战修订).

    Returns:
        dict:
          "lsb_0bit": [(label, preview_str, has_keyword), ...]   # 12 entries
          "single_8bit": [(label, preview_str, has_keyword), ...]  # 3 entries
    """
    # 段 1: LSB 0 bit (12 多通道, bit=0 字节流)
    lsb_0bit = []
    for label, channels in _LSB_12_CHANNELS:
        byte_stream = _extract_lsb_byte_stream_zero_aware(
            img_array, channels=channels, bit=0,
            scan_order=scan_order, byte_bit_order=byte_bit_order,
        )
        preview = _ascii_preview(byte_stream, n_bytes=n_bytes)
        has_kw = any(kw in byte_stream[:n_bytes] for kw in _PREVIEW_KEYWORDS)
        lsb_0bit.append((label, preview, has_kw))

    # 段 2: 单通道 0-7 bit (3 单通道, 完整 8 bit plane 字节流)
    single_8bit = []
    for label in _SINGLE_3_CHANNELS:
        ch_idx = _channel_index(label)
        plane = img_array[:, :, ch_idx]
        byte_stream = _channel_8bit_byte_stream(plane)
        preview = _ascii_preview(byte_stream, n_bytes=n_bytes)
        has_kw = any(kw in byte_stream[:n_bytes] for kw in _PREVIEW_KEYWORDS)
        single_8bit.append((label, preview, has_kw))

    return {"lsb_0bit": lsb_0bit, "single_8bit": single_8bit}
```

### 3.3 `_format_lsb_2segment_for_journal`

```python
def _format_lsb_2segment_for_journal(matrix):
    """2 段 LSB matrix 渲染 (per Owner v4 期望).

    输出格式:
        ===LSB_TOOL===

        LSB 0 bit
        RGB:Hey! ... <==
        ... (12 行)

        单通道0-7bit
        R:........
        ... (3 行)
    """
    if not matrix:
        return ""

    lines = ["===LSB_TOOL===", ""]

    lines.append("LSB 0 bit")
    for label, preview, has_kw in matrix.get("lsb_0bit", []):
        marker = "  <==" if has_kw else ""
        lines.append(f"{label}:{preview}{marker}")
    lines.append("")

    lines.append("单通道0-7bit")
    for label, preview, has_kw in matrix.get("single_8bit", []):
        marker = "  <==" if has_kw else ""
        lines.append(f"{label}:{preview}{marker}")

    return "\n".join(lines)
```

### 3.4 `_run_detect` 集成

```python
def _run_detect(self, arr, file_path):
    sps = []
    # ... 现有 detect 逻辑 (preset 命中 SP, 3 mode 维度) ...

    # v4 实战: 2 段 LSB matrix
    matrix = _build_lsb_2segment_matrix(arr, n_bytes=50)
    matrix_text = _format_lsb_2segment_for_journal(matrix)

    return ActionResult(
        success=True,
        data={...},
        message=matrix_text,  # 直接作为 message (简洁, 不带 lsb_tool detect: preset=... 前缀)
    )
```

---

## 4. 测试计划

### 4.1 单元测试 (改 ~50 行, 净 -20)

**`tests/unit/core/actions/test_lsb_tool_common.py`** 改:
- `TestAsciiPreview`: 保留 (utility function)
- **删**: `TestBuildBitPlanePreviewMatrix` (v3 替代品)
- **删**: `TestFormatMatrixForJournal` (v3 替代品)
- **新增**: `TestBuildLsb2SegmentMatrix` (6 个 case: 2 段结构 + 12 通道顺序 + 3 单通道顺序 + n_bytes + synthetic RGB 命中 + 关键字标记)
- **新增**: `TestFormatLsb2SegmentForJournal` (3 个 case: 基本格式 + 关键字 marker + 空 matrix)

### 4.2 集成测试 (改 ~5 行)

**`tests/integration/test_lsb_tool_regression.py`** 改:
- `test_lsb_tool_detect_2segment_format` (期望 2 段结构 + `===LSB_TOOL===` + 2 个段标题 + 12 多通道 + 3 单通道)
- `test_lsb_tool_detect_synthetic_2segment_keyword` (期望 `Hey` 命中 + `<==` 标记)

### 4.3 Smoke test (实战)

**synthetic 'Hey!' PNG** (per Owner 期望):
```
===LSB_TOOL===

LSB 0 bit
RGB:Hey!........  <==          ← 期望命中
BRG:$3..........
RBG:$S.@........
... (12 通道)
00B:..I ........

单通道0-7bit
R:................................
G:................................
B:................................
```

---

## 5. 风险评估

| 风险 | 等级 | 缓解 |
|---|---|---|
| v4 简洁输出丢 mid-bit 信息 (bit 1~6) | 🟢 低 | Owner 实战确认 "1-7bit 位忽略" (v4 22:13 反馈);mid-bit 实战 < 5% |
| v4 不再显示 "lsb_tool detect: preset=..." 前缀 | 🟢 低 | `===LSB_TOOL===` 总标题已标识工具 |
| 3 单通道 8 bit plane 字节流跟多通道 bit 0 字节流混淆 | 🟢 低 | 中文段标题 "LSB 0 bit" vs "单通道0-7bit" 明确区分 |

---

## 6. 提交策略 (v4 最终, per AGENTS §2.1 单 PR ≤ 400 行)

**main 6 commits 落地 + 即将 7 commits** (per §6):

| Commit | 说明 | LOC |
|---|---|---|
| `f23be60` | Commit 1: `_extract_lsb_byte_stream_zero_aware` + 9 单测 | +265/-2 |
| `38f93a9` | Commit 2: v2 `_build_15channel_preview_matrix` + `_format_15channel_matrix_for_journal` (已删 v4) | +331/-2 |
| `82a34db` | Commit 3: v2 `_run_detect` KEEP+ADD 集成 (已删 v4) | +78/-1 |
| `1119992` | Commit 4: docs(upgrade.md) 状态更新 | +2/-2 |
| `1453ba2` | Commit 5: v3 8 bit × 15 channel 实战修订 | +133/-400 |
| `52833d4` | Commit 6: v3 docs | +314/-1 |
| **`(pending)`** | **Commit 7 (v4 实战最终)**: 2 段 LSB matrix + 删 v3 旧实现 + 单测改 | ~+200/-180 |

---

## 7. 决策点 (v4 最终, Owner 22:13 + 22:16 实战拍板)

| # | 决策点 | v4 拍板 |
|---|---|---|
| **Q1** | 整体结构? | **2 段** (`===LSB_TOOL===` + LSB 0 bit 段 + 单通道0-7bit 段) |
| **Q2** | 单通道 R/G/B 显示? | **全 8 bit 字节流** (per v0.5-train-010 N=NP 模式) |
| **Q3** | 12 通道顺序? | **Owner 列表顺序**: RGB/BRG/RBG/BGR/GRB/GBR/RG0/R0B/0GB/R00/0G0/00B |
| **Q4** | `<==` 标记 + preview 字节数? | **`<==` 保留, 50 byte** |

---

## 8. 时间线（实际）

| 阶段 | 实际耗时 |
|---|---|
| Commit 1 (zero-aware byte stream) | ~30 min |
| Commit 2 (v2 15ch matrix, 已删) | ~30 min |
| Commit 3 (v2 KEEP+ADD 集成, 已删) | ~20 min |
| Commit 4 (docs) | ~5 min |
| Commit 5 (v3 8×15 matrix) | ~30 min |
| Commit 6 (v3 docs) | ~5 min |
| **Commit 7 (v4 2-segment 最终) ~** | **~30 min** |
| **总计** | **~2.5 h** |

---

## 9. 关联 spec

- **触发**: `v0.5-train-016-lsb-15channel-matrix.md`
- **前置**: `v0.5-lsb-tool-unify` (Phase 1-4 已落地)
- **替代**: `v0.5-lsb-tool-bitplane-preview-matrix` (8 bit × 6 perm 函数被 v4 删)
- **历史**: v1/v2/v3 4 次实战修订记录在本 spec §1.1
- **回归**: 跑 v0.5-train-009/010/011/014/016 实战题验证不回归

---

> **本 spec v4 已实施, 待 Owner Y 后 push 走 §2.4 询问卡**