# v0.5-zipcrack-doc-update — tools.md zipcrack.py 文档与实际状态不一致修正

> **创建时间**：2026-06-28 14:05
> **状态**：🟡 design（per AGENTS.md §1 铁律 2 文档先行；Owner 审完走实施）
> **触发**：Owner 2026-06-28 14:03 实战反馈 `QR_code__foremost/zip/00000000.zip` 暴力破解成功（password='7639', tried 7640/8421616），但 tools.md §3.9 仍标 `zipcrack.py ❌ 未安装`
> **Owner 拍板**：B 完整方案（per spec 起草 + 实施 + upgrade.md 索引）

---

## 1. 背景

### 1.1 文档与实际状态不符

**tools.md §3.9 当前内容**（line 268）:

```markdown
| **zipcrack.py** | ❌ | `misc/zipcrack.py`（未软链）| 纯 Python zip 4-6 位爆破 | v0.1 作为 john 缺失的 fallback |
```

**实际情况**（per Owner 2026-06-28 14:03 实战 journal）:

```
=== Chain: bruteforce_zip ===
=== File: .../QR_code__foremost/zip/00000000.zip
[1] bruteforce_zip OK FOUND password='7639' (tried 7640/8421616)
```

**根因诊断**:

1. `zipcrack.py` 文件**从未存在**：
   - `glob **/zipcrack*` 验证 0 文件命中
   - tools.md 描述的 "未软链" 是错的 —— 不是软链问题，是根本没创建过
   - 这是 v0.1 计划要写的脚本，v0.5+ 没做

2. automisc 实际用 **自研 Python 字典爆破**（替代了原本计划的 zipcrack.py）：
   - `src/automisc/core/actions/zip_chain.py:402 _generate_passwords()` —— 纯 stdlib `itertools.product`
   - 字典范围：**数字 4-6 位 + 字母 4 位** ≈ 8.4M 组合
   - Owner 实战命中 `tried 7640/8421616` 跟 `_generate_passwords` 注释 "10^4 + 10^5 + 10^6 + 52^4 ≈ 8.4M" **完全一致**

3. **Narrative 错**：`tools.md:380` §6.1 row 18 john 备注说 "Win 可用 hashcat / zipcrack.py 替代" —— 实际 automisc 用的是自研 Python，不是 zipcrack.py

### 1.2 Owner 实战反馈（per 2026-06-28 14:03）

> "tools.md 3.9 zipcrack.py 的状态是未安装，但我点击左侧工具栏，实际上是成功暴力破解了zip文件的"
> "zipcrack.py 的状态在文档中是否应该更新？"

### 1.3 参考模板

`tools.md:373` §6.1 row 10 zsteg 已经是正确的"自研替代"叙事模板：

```markdown
| 10 | **zsteg** | Stego/Image | ❌ pending（Win 无 Ruby gem；用自研 `lsb_tool` 替代，per [`upgrade/v0.5-lsb-tool-unify.md`](./upgrade/v0.5-lsb-tool-unify.md)，3 mode 统一 LSB 工具 detect/extract/extract_bytes）|
```

**zipcrack 应该照抄此模板**。

---

## 2. 范围

### 2.1 改动文件清单

| 类别 | 文件 | 操作 | LOC 影响 |
|---|---|---|---|
| **新 spec** | `upgrade/v0.5-zipcrack-doc-update.md` | 新建（本文件）| +150 |
| **改 doc** | `tools.md:268` §3.9 row | 改：zipcrack.py ❌ → 自研 bruteforce_zip ✅ + 引用本 spec | -1/+1 |
| **改 doc** | `tools.md:348` §5 Archive row | 改：删 zipcrack.py 提及 | -1 |
| **改 doc** | `tools.md:380` §6.1 row 18 john | 改：备注 "Win 可用 hashcat / zipcrack.py 替代" → "Win 用自研 Python 字典爆破 (`BruteforceZipAction`)" | -1/+1 |
| **改 doc** | `tools.md:380` §6.1 表格 | 改：加 row (新编号) bruteforce_zip (自研) ✅ 已装 | +1 |
| **改 doc** | `upgrade.md` 索引 | 加本迭代行 | +2 |

**净 LOC**: ~ +155 行（大部分是 spec）

### 2.2 不动

- `src/automisc/core/actions/zip_chain.py` — **不动**（自研实现已 work，Owner 实战验证）
- `_generate_passwords()` 算法 — **不动**（8.4M 字典覆盖实战 CTF 4-6 位数字密码）
- `src/automisc/tools/misc/archive/` — **不动**（zip_classify / unzip / john / sevenz adapter 状态不变）
- 真实 `zipcrack.py` 文件 — **不创建**（per v0.1 计划已 deprecated，自研 fallback 足够）

---

## 3. 详细设计

### 3.1 tools.md §3.9 改动

**现状**（line 261-269）:

```markdown
| 工具 | 状态 | 路径 / 安装 | 用途 | 备注 |
|---|---|---|---|---|
| **7z** | ✅ | `extend-tools/bin/win-x64/7z.exe` | 全格式解压（zip/rar/7z/tar.gz/bz2/xz） | P0 · v0.1 必须 |
| **unzip** | ❌ | `/usr/bin/unzip` | zip 解压 + 伪加密检查 | P0 |
| **file** | ✅ | `extend-tools/bin/win-x64/file.exe` | magic 识别压缩类型 | P0 |
| **john** | ❌ | `brew install john-jumbo` | 4-6 位数字爆破（zip/rar） | P0 · v0.1 必须装 |
| **hashcat** | ❌ | `brew install hashcat` | GPU 加速破解（zip/rar 哈希） | v0.5 安装 |
| **zipcrack.py** | ❌ | `misc/zipcrack.py`（未软链）| 纯 Python zip 4-6 位爆破 | v0.1 作为 john 缺失的 fallback |
| **rar / unrar** | ❌ | `brew install rar` | rar 创建 / 解压（区别于 unrar-only） | v0.5 安装 |
```

**新版本**:

```markdown
| 工具 | 状态 | 路径 / 安装 | 用途 | 备注 |
|---|---|---|---|---|
| **7z** | ✅ | `extend-tools/bin/win-x64/7z.exe` | 全格式解压（zip/rar/7z/tar.gz/bz2/xz） | P0 · v0.1 必须 |
| **unzip** | ❌ | `/usr/bin/unzip` | zip 解压 + 伪加密检查 | P0 |
| **file** | ✅ | `extend-tools/bin/win-x64/file.exe` | magic 识别压缩类型 | P0 |
| **john** | ❌ | `brew install john-jumbo` | 4-6 位数字爆破（zip/rar） | P0 · v0.1 必须装 |
| **hashcat** | ❌ | `brew install hashcat` | GPU 加速破解（zip/rar 哈希） | v0.5 安装 |
| **bruteforce_zip (自研)** | ✅ | `core/actions/zip_chain.py:BruteforceZipAction` + `_generate_passwords` | 纯 Python 字典爆破 zip 4-6 位数字 + 4 位字母 (8.4M 组合) | v0.5 自研替代未实现的 zipcrack.py；per [`upgrade/v0.5-zipcrack-doc-update.md`](./upgrade/v0.5-zipcrack-doc-update.md) |
| **rar / unrar** | ❌ | `brew install rar` | rar 创建 / 解压（区别于 unrar-only） | v0.5 安装 |
```

### 3.2 tools.md §5 Archive row 改动

**现状**（line 348）:

```markdown
| **Archive** | 7z + unzip + file | john（爆破）/ hashcat（GPU 爆破）/ zipcrack.py |
```

**新版本**:

```markdown
| **Archive** | 7z + unzip + file + bruteforce_zip (自研 Python 字典爆破) | john（爆破）/ hashcat（GPU 爆破）|
```

### 3.3 tools.md §6.1 改动

**现状 row 18**（line 380）:

```markdown
| 18 | **john** | Misc/Archive | ❌ pending（Win 可用 `hashcat` / `zipcrack.py` 替代）|
```

**新版本**:

```markdown
| 18 | **john** | Misc/Archive | ❌ pending（Win 用自研 `BruteforceZipAction` Python 字典爆破替代, per [`upgrade/v0.5-zipcrack-doc-update.md`](./upgrade/v0.5-zipcrack-doc-update.md)）|
```

**新增 row**（接在 row 18 john 后面）:

```markdown
| 19 | **bruteforce_zip** | Misc/Archive | ✅ 已装（自研 `core/actions/zip_chain.py:BruteforceZipAction` + `_generate_passwords`，纯 stdlib `itertools.product`，8.4M 字典；per [`upgrade/v0.5-zipcrack-doc-update.md`](./upgrade/v0.5-zipcrack-doc-update.md)）|
```

注意：原 §6.1 末尾有 "21 个工具"，加 1 个变 22 个。需要看是否 §6 头部数字也要改。

### 3.4 upgrade.md 索引加行

```markdown
| v0.5-zipcrack-doc-update | tools.md zipcrack.py 文档与实际状态不一致修正：zipcrack.py 从未存在, automisc 用自研 `BruteforceZipAction` (Python 字典爆破 8.4M 组合) 替代;tools.md §3.9 / §5 / §6.1 改写 | ✅ done | (本 commit) | [`upgrade/v0.5-zipcrack-doc-update.md`](upgrade/v0.5-zipcrack-doc-update.md) |
```

---

## 4. 风险评估

| 风险 | 等级 | 缓解 |
|---|---|---|
| 改 §6.1 row 编号 (#18 / #19) 影响现有引用 | 🟢 低 | §6.1 表格按 subflow 排序，row 编号仅引用，未在代码 / spec 里 hardcode |
| 文档与现实仍有不一致（如 john / hashcat 仍未装但叙事完整）| 🟢 低 | john / hashcat 标 ❌ pending 是事实，文档叙事正确 |
| 错过其他类似的"未实现外部工具"列在 docs 里 | 🟡 中 | 本 spec 范围限定 zipcrack.py；其他工具现状靠 Owner 实战反馈触发（per 训练驱动迭代 §5）|

---

## 5. 提交策略（per AGENTS §2.1 单 PR ≤ 400 行）

**单 PR**（1 commit + doc 改动 + push 走 §2.4 询问卡）:

1. **Commit 1**: spec 创建 + tools.md §3.9 / §5 / §6.1 改写 + upgrade.md 索引

**净 LOC**: ~ +155 行（含 spec），符合 §2.1 单 PR ≤ 400 行。

**push**: 询问卡 → 等 Owner Y → `git push`

---

## 6. 决策点（已拍板）

| # | 决策点 | 拍板 |
|---|---|---|
| Q1 | 是否新建 spec？ | ✅ B 完整方案（per Owner 2026-06-28 14:05） |
| Q2 | 是否加 §6.1 row 19 bruteforce_zip？| ✅ 是（与 zsteg row 10 对称）|

---

## 7. 时间线（目标）

| 阶段 | 预计耗时 | Owner 签字点 |
|---|---|---|
| 写 spec + 改 doc + 改 upgrade.md 索引 + commit | 15 min | (不签字, 自审) |
| §2.4 询问卡 push | <1 min | Owner Y |

---

## 8. 关联 spec

- **触发**: Owner 2026-06-28 14:03 实战反馈（`QR_code__foremost/zip/00000000.zip` bruteforce_zip 实战命中 password='7639'）
- **不触发架构升级**：自研实现 + 文档同步，不动核心代码
- **回归**: 不需 regression test（纯 doc 改动）

---

> **本 spec 等 Owner 拍板 §6 决策点后启动实施 → push 走 §2.4 询问卡**