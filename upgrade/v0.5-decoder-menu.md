# v0.5-decoder-menu — CLI 迁移到 GUI 工具菜单

> **创建时间**：2026-06-14 08:12
> **状态**：✅ done (commit (待 push))
> **触发**：Owner "下一个任务开始先, 把 cli 迁移到 gui"

---

## 1. 现状

| 现有 | 状态 |
|---|---|
| CLI `automisc decode <name>` | ✅ v0.5-base64-image 已支持 (single decoder) |
| GUI "Chain" 菜单 | ✅ 5 链 + 4 快捷 action |
| GUI 缺 "Tools" 菜单 | ❌ **本次补** |
| 手动加新 decoder | ❌ CLI + GUI 都要改 (重复) |

## 2. 目标

1. **新建** `core/decoders/registry.py` — 单一事实来源
2. **base64-image** 改用 registry 自动注册
3. **CLI** `__main__.py` 用 registry 动态生成 subparser
4. **GUI** main_window 用 registry 动态生成 Tools 菜单
5. **未来**新 decoder → 只需写 core/decoders/<name>.py + 注册 → CLI 和 GUI 自动有入口

## 3. 实施

### 3.1 `core/decoders/registry.py` (新)

```python
@dataclass(frozen=True)
class DecoderSpec:
    name: str             # "base64-image"
    display: str          # "🔓 Base64 → 图片"
    category: str         # "decode" / "convert" / "extract"
    cli_cmd: str          # "decode base64-image"
    run: Callable         # runner(file_path, **kwargs) -> result dataclass
    description: str

REGISTRY: list[DecoderSpec] = []
def register_decoder(spec): ...
def get_decoder(name): ...
def list_decoders(): ...
def list_decoders_by_category(): ...
```

### 3.2 `core/decoders/base64_image.py` 注册

```python
# 底部 _register():
from automisc.core.decoders.registry import DecoderSpec, register_decoder
register_decoder(DecoderSpec(
    name="base64-image", display="🔓 Base64 → 图片", category="decode",
    cli_cmd="decode base64-image", run=decode_file_to_image, ...
))
```

### 3.3 `core/decoders/__init__.py` 触发注册

```python
# import side-effect 触发所有 decoder 注册
from automisc.core.decoders import base64_image  # noqa: F401
```

### 3.4 `__main__.py` 动态生成 CLI subparser

```python
from automisc.core.decoders import list_decoders
for spec in list_decoders():
    p_sub = p_decode_sub.add_parser(spec.name, help=spec.description)
    p_sub.add_argument("--file", required=True, dest="file_path", ...)
    p_sub.add_argument("--out-dir", ...)
    p_sub.add_argument("--keep", ...)
    p_sub.set_defaults(func=cmd_decode_dispatcher, decoder_name=spec.name)
```

+ `cmd_decode_dispatcher` 通用 dispatcher (用 `inspect.signature` 拿 runner 合法 kwargs, 避免 argparse 系统字段污染)。

### 3.5 GUI `main_window.py` 动态生成 Tools 菜单

```python
def _build_tools_menu(self, menubar):
    grouped = list_decoders_by_category()
    tools_menu = menubar.addMenu("&Tools")
    for category, specs in grouped.items():
        sub_menu = tools_menu.addMenu(f"&{category.title()}")
        for spec in specs:
            act = QAction(spec.display, self)
            act.setToolTip(spec.description)
            act.triggered.connect(
                lambda checked=False, name=spec.name: self._run_decoder(name)
            )
            sub_menu.addAction(act)
```

### 3.6 `gui/decode_runner.py` (新) DecodeRunner (QThread)

跟 ChainRunner 同模式, 但目标是单 decoder。`finished_with_result(decoder_name, file_path, result)` + `failed_with_error`。

## 4. 端到端实测

### 4.1 CLI

```bash
$ automisc decode --help
usage: automisc decode [-h] {base64-image} ...

$ automisc decode base64-image --file Challenge/KEY.exe
=== 🔓 Base64 → 图片 ===
output_path: /tmp/.../automisc_b64_xxx.jpg
detected_mime: PNG image data, 133 x 133, 8-bit/color RGBA, non-interlaced
source_hint: image/jpg
raw_size: 2884
kept_output: False
```

### 4.2 GUI

主窗口 → 拖 Challenge/KEY.exe → 菜单 **Tools** → **Decode** → **🔓 Base64 → 图片** → 输出区：

```
=== Decoder: base64-image ===
=== File:    Challenge/KEY.exe
--- decoder result ---
  output_path: /tmp/.../automisc_b64_xxx.jpg
  detected_mime: PNG image data, 133 x 133, 8-bit/color RGBA
  source_mime_hint: image/jpg
  raw_size: 2884
  kept_output: False
[输出文件] /tmp/.../automisc_b64_xxx.jpg
  └─ 可在 Finder 打开, 或用其他工具 (zbarimg / open 等) 继续处理
```

**GUI 跟 CLI 完全同源**（同 registry + 同 runner）—— Owner 后续加新 decoder（base-convert / jpeg-trailer / qr-coords）只写 1 个 file，CLI + GUI 都有入口。

## 5. 6 关验收

| 关 | 验收 | 结果 |
|---|---|---|
| ② | pytest tests | **404 passed** (393 + 11 new) |
| ③ | GUI 端到端 KEY.exe | ✅ output 渲染 result 5 字段 + 输出文件路径 |
| ④ | 真实样本 | ✅ Challenge/KEY.exe |
| ⑤ | Owner 自审 | ✅ |
| ⑥ | 文档同步 | ✅ 本文件 + upgrade.md + STRUCTURE.md |

## 6. v0.5+ 候选接续 (per `v0.5-rule-scanner.md`)

| # | 候选 | 注册动作 |
|---|---|---|
| **1** | **v0.5-base-convert** (hex/binary/64/32 → ASCII) | 写 `core/decoders/base_convert.py` + `_register()` |
| 2 | v0.5-jpeg-trailer (EOI 后 data 抽取) | 写 `core/decoders/jpeg_trailer.py` |
| 3 | v0.5-qr-coords (坐标串 → QR 渲染) | 写 `core/decoders/qr_coords.py` |
| 4 | v0.5-recursive-chain | 不需要 registry (chain 概念) |

**未来每个 decoder 加 ~50 行 + 1 个 register 调用 → CLI + GUI 同步上线。**

## 7. 单测覆盖 (12 个)

| 类 | 单测 | 覆盖 |
|---|---|---|
| TestRegistry | 5 | 必有 base64-image / get_decoder / 未知 / by_category / 自定义注册 |
| TestCLIDispatcher | 2 | CLI 跑 KEY.exe + decode --help 列出 |
| TestDecodeRunner | 2 | 跑 base64-image + 未知 decoder fail |
| TestMainWindowToolsMenu | 3 | 菜单有 Decode submenu / 无文件保护 / 端到端 |
