# v0.5-tool-install-batch-2 — sox 装上 + evtx_dump Rust 二进制文档化

> **创建时间**：2026-06-14 16:35
> **状态**：✅ done（2026-06-14 16:45 二次更新：去掉 python-evtx 历史背景，专注 Rust 工具 + 封装指南）
> **触发**：Owner "实际运行一下 extend_tools/evtx_dump... 对了sox也装了... 不用给evtx_dump解释原来的是用的python，直接解释rust版本这个工具的路径，以及简单的使用说明，比如什么参数对应什么功能（方便后面项目对工具的封装使用）"

---

## 1. 范围（Owner 指定）

1. 运行 `extend_tools/evtx_dump`，**直接当 Rust 工具解释**：路径 + 参数清单 + 封装建议
2. 不讲 python-evtx 的历史（避开容易混的背景）
3. sox 已装，更新 tools.md 状态

---

## 2. 调查结果

### 2.1 extend_tools/evtx_dump 实际是啥

```
$ file extend_tools/evtx_dump
Mach-O 64-bit executable x86_64

$ extend_tools/evtx_dump --version
EVTX Parser 0.8.2

$ extend_tools/evtx_dump --help
Utility to parse EVTX files
Usage: evtx_dump [OPTIONS] <INPUT>
```

**结论**：`extend_tools/evtx_dump` 是 **Rust crate `evtx` v0.8.2 的 macOS x86_64 二进制**。

### 2.2 sox

```
$ which sox
/usr/local/bin/sox

$ sox --help
SoX v
Usage summary: [gopts] [[fopts] infile]... [fopts] outfile [effect [effopt]]...
```

✅ sox 已装（brew，SoX v14.x）。

---

## 3. evtx_dump 参数清单（per `evtx_dump --help`）

> 完整列表见 [`tools.md §3.4.1`](./tools.md) "参数速查" 表。

### 3.1 核心参数（5 个，封装必用）

| 参数 | 默认 | 说明 |
|---|---|---|
| `<INPUT>` | （必填）| .evtx 文件路径 |
| `-o, --format` | `xml` | 输出格式：`xml` / `json` / `jsonl` |
| `-f, --output` | stdout | 写到文件而不是 stdout（写文件前会问确认）|
| `--no-confirm-overwrite` | false | 跳过 `-f` 写文件的覆盖确认（**自动化必加**）|
| `-t, --threads` | 0 (=CPU) | worker 线程数（保持默认即可）|

### 3.2 可选参数（10 个，按需）

- `--events <RANGES>` — 抽样调试（`0-10` / `20-30` 等）
- `--validate-checksums` — CTF 场景**不推荐**（损坏文件反而记录更多）
- `--no-indent` — JSONL 自动启用
- `--separate-json-attributes` — JSON 模式：XML 属性放 `_attributes` 对象
- `--dont-show-record-number` — JSONL 自动启用
- `--ansi-codec <C>` — 默认 `windows-1252`，CJK 试 `utf-8` / `gbk` / `gb18030`
- `--stop-after-one-error` — debug 用
- `-v` / `-vv` / `-vvv` — info / debug / trace 日志（trace 仅 debug build）
- `-V, --version` / `-h, --help`

---

## 4. 典型调用（5 个 pattern · 见 `tools.md §3.4.2`）

```bash
# 1) XML 到 stdout（默认，最简，GUI 可直接 grep）
evtx_dump file.evtx | grep -E 'flag|key|password|ctf' -i

# 2) JSONL 到文件（journal 结构化落盘）
evtx_dump file.evtx -o jsonl --no-indent --dont-show-record-number -f out.jsonl --no-confirm-overwrite

# 3) 仅看前 10 个事件（快速 triage）
evtx_dump file.evtx --events=0-10

# 4) CJK Windows evtx
evtx_dump file.evtx --ansi-codec=utf-8

# 5) 损坏 evtx 全捞（跳过 checksum 校验）
evtx_dump file.evtx | head -1000
```

---

## 5. adapter 封装模板（per Architecture §6 plug-in · 见 `tools.md §3.4.3`）

```python
# src/automisc/tools/forensics/log/evtx_dump.py
from pathlib import Path
from ...base import ToolAdapter, ToolResult

class EVTXDumpAdapter(ToolAdapter):
    """evtx_dump (Rust evtx crate 0.8.2) — Windows .evtx parser"""
    name = "evtx_dump"
    binary = "extend_tools/evtx_dump"  # 相对 repo root，subprocess 走绝对路径

    def run(self, file_path: Path, **kwargs) -> ToolResult:
        fmt = kwargs.get("format", "xml")           # xml / json / jsonl
        out_file = kwargs.get("out_file")           # None = stdout
        threads = kwargs.get("threads", 0)          # 0 = CPU 自动

        cmd = [self.binary, str(file_path), "-o", fmt]
        if threads:
            cmd += ["-t", str(threads)]
        if out_file:
            cmd += ["-f", str(out_file), "--no-confirm-overwrite"]

        # subprocess.run(cmd, capture_output=True, timeout=60, text=True)
        # → ToolResult(stdout=..., stderr=..., returncode=...)
```

**封装决策点**（未来 adapter 实装时定）：

| 决策点 | 建议 | 理由 |
|---|---|---|
| binary 路径 | 相对 repo root `extend_tools/evtx_dump` | per AGENTS §2.3 macOS 标准 PATH 简洁 |
| 默认 format | `xml` | CLI grep 友好 |
| threads 默认 | `0`（auto）| 多核机器加速 |
| timeout | `60s` 起，跑挂再加 | 大文件可能 60s+ |
| 部分失败行为 | **不要 fail-fast** | evtx_dump 对损坏文件部分成功（部分 chunk 输出 + 部分 skip） |

---

## 6. tools.md 状态更新

| 节 | 工具 | 旧 | 新 |
|---|---|---|---|
| §3.4 Log Forensics | **evtx_dump** | ⚠️ | ✅ 走 `extend_tools/evtx_dump`（Rust 0.8.2）+ 参数速查表 + adapter 模板 |
| §3.4 Log Forensics | **python-evtx** | ⚠️ | ✅ 包 0.8.1 已装；CLI 走 extend_tools 二进制 |
| §3.6 Audio Stego | **sox** | ❌ | ✅ brew install |
| §4 Python 包 | **python-evtx** | ⚠️ | ✅ |
| §6.1 P0 | **evtx_dump** (#16) | ⚠️ | ✅ |
| §6.1 P0 | **sox** (#20) | ❌ | ✅ |
| §2 总表 | Forensics/Steganography | 34✅/4⚠️ | 37✅/2⚠️ |
| §8 变更日志 | — | — | 新增 2.4 条目 |

---

## 7. 验证清单（per AGENTS.md §1 铁律 4 简化版）

- [x] **6 关 1 — 代码合并 main**：N/A（无代码改动）
- [x] **6 关 2 — pytest 全绿**：N/A
- [x] **6 关 3 — GUI 集成**：N/A
- [x] **6 关 4 — 真实样本 smoke**：
  - `extend_tools/evtx_dump --help` ✅
  - `extend_tools/evtx_dump --version` → `EVTX Parser 0.8.2` ✅
  - `sox --help` ✅
- [x] **6 关 5 — Owner 自审**：本次由 Owner 直接触发 + 二次审查（"不要 python 解释"）
- [x] **6 关 6 — 文档同步**：tools.md §3.4/§3.6/§4/§6.1/§2/§8 + upgrade.md 索引 ✅

---

## 8. commit 模板（等 Owner 说 push）

```
[v0.5-tool-install-batch-2] sox brew 装 + evtx_dump Rust 0.8.2 文档化 (路径/参数/adapter 模板)

- tools.md §3.6 sox: ❌ → ✅ (brew install)
- tools.md §3.4 evtx_dump: ⚠️ → ✅ (extend_tools/evtx_dump Rust 0.8.2)
- tools.md §3.4 加 evtx_dump 参数速查表 (14 个参数, 含默认值+封装建议)
- tools.md §3.4 加 5 个典型调用 pattern
- tools.md §3.4 加 adapter 封装模板 (Python 代码, per Architecture §6 plug-in)
- tools.md §3.4 / §4 python-evtx: ⚠️ → ✅ (包 0.8.1 已装)
- tools.md §6.1 P0 #16 (evtx_dump) + #20 (sox): 状态更新
- tools.md §2 总表 + §8 变更日志 (2.4): 数字同步 + 新条目
- per Owner 2026-06-14 16:35 "不用给evtx_dump解释原来的是用的python，直接解释rust版本"
```

---

## 9. 变更日志

| 日期 | 变更 |
|---|---|
| 2026-06-14 16:35 | 初版：sox + evtx_dump 调查 + tools.md 6 处状态更新 |
| 2026-06-14 16:45 | 二次更新：去掉 python-evtx 历史背景；加 evtx_dump 完整参数速查表 + 5 典型调用 + adapter 封装模板（per Owner 反馈"直接解释 rust 版本"）|